# 深入验证与修复（2026-09-18）

本轮针对本仓库 OURS 收益模块和独立平台 Treasury，采用先保留失败用例、再修复、最后完整回归的顺序。所有交易仅运行本地测试链，没有公开链部署或真实资金操作。

## 已确认的问题

| 问题 | 修复前证据与影响 | 修改 |
|---|---|---|
| 撤销股票资格未阻止继续加 LP | 股票同时属于 feeAsset 时，撤销 stockAsset 后 `_knownAsset` 仍返回 true；回归用例原本要求拒绝，却成功加仓。需已有执行权限和有效签名，不是普通地址直接盗币 | 每次 `addLiquidity` 重查至少一边仍具备股票资产资格。已有仓位仍可按治理队列退出 |
| 原生币结算受 Hook 同步状态影响 | 在未修改的官方 V4 核心上，测试 Hook 于同一交易同步 ERC20，原适配器原生币结算报 `NonzeroNativeValue()`（`0xb0ec849e`）。交易回滚，属于可用性问题 | `OursV4Adapter` 支付原生币前显式 `sync(address(0))`，清除其他币种同步状态 |
| 放弃治理权会永久丢失部分资产退出权限 | 原继承的 `renounceOwnership` 能成功把 owner 置零，而保留 Token 和 LP 的退出需要 owner 排队；拒绝放弃的回归断言先失败 | Treasury 禁止放弃所有权，保留两步治理权转移。此项属于防止管理员误操作的托管加固，不是未经授权的提款漏洞 |

修复没有改动 30/70、平台收入 80/20、用户自行领取分红等规则。未发现测试范围内可复现的无权限地址盗取已记账资产路径；这不构成不存在其他漏洞的证明。

## 新增验证维度

- **独立账本状态机**：平台 3 个固定种子，每种 60 步，交错入账、兑换、保留、建仓、退出、收费、领取和越权操作。每步核对独立预期账本、实际 Token 余额、LP 数量、平台固定收款人和总量守恒。连同原项目 144 步，共 324 步；这是有限可复现状态测试，不是形式化证明。
- **治理生命周期**：两步交接、旧治理失权、操作员撤销、ERC1271、队列取消与重新计时、资产冻结后的失败回滚与恢复、暂停和撤销白名单后的已授权退出。
- **官方 V4 核心**：双方向 ERC20 兑换、原生币兑换、价格限制下部分成交、输出下限失败、实际集中流动性建仓、实际交易产生手续费、收取手续费、延时移除。
- **仓位与回滚**：移除 LP 时不满足最小回收金额，必须同时恢复队列、账本和官方核心仓位；另一个调用合约使用相同 ticks/salt 也不能移走 Treasury 拥有的仓位。
- **已有回归**：收入来源认证、项目和历史策略隔离、重入、伪造到账、残留授权、burn 检查、签名重放、分红冒领/重复领取、万人清单和两个部署 CLI 继续全量运行。

真实 V4 测试使用 `@uniswap/v4-core@1.0.2` 中的官方 PoolManager 源码，按其要求用 `solc 0.8.26` 编译，部署到 Hardhat 2.26.3 的本地 Cancun EVM。官方核心源码没有修改。流动性提供者和同步 Hook 为测试夹具；Hook 使用本地 RPC 在权限位地址安装测试字节码。它不是主网 fork，也不是真实 PONS Hook。

生产合约继续使用 Solidity 0.8.30、optimizer 200、viaIR、EVM paris；原业务和异常场景继续在 Ganache 中运行。具体依赖版本及完整性摘要锁定在 package-lock.json。

## 运行方式

```sh
npm ci --ignore-scripts
npm run check          # 编译 + 全部业务测试 + 部署 CLI + 官方 V4 核心测试
npm run test:deep      # 平台扩展测试
npm run test:v4        # 编译并运行官方 V4 核心测试
```

## 完整回归结果

`npm run check` 退出码 0：**77 项通过、0 失败、0 跳过**，包括 66 项业务/清单/安全/平台测试、1 项同时演练两个部署 CLI 的测试、10 项官方 V4 核心和托管回归测试。原始输出见 [完整日志](reports/deep-check-2026-09-18.log)。

本轮新增 18 项测试。`OursV4Adapter` 部署后代码为 5,331 字节，`OursPlatformTreasury` 为 18,081 字节，均低于 EIP-170 上限。新增 JavaScript 文件通过 Node 语法检查。编译器对始终回滚的 `renounceOwnership` 给出可缩紧为 view 的建议，不影响编译或上述权限检查。

完整回归之后，将新增编译器别名 `solc-v4` 的 `tmp` 依赖固定为 0.2.7，消除该路径旧版临时文件库告警；没有改变编译器或合约源码。随后 `npm ci --ignore-scripts` 干净安装成功，重新编译官方核心并运行其 10 项测试，全部通过，见 [依赖修正后复测](reports/v4-final-2026-09-18.log)。没有把这次 10 项复测重复计入 77 项。

## 测试工具依赖审查

最终 `npm audit --json` 仍为非零：45 个依赖条目告警（Critical 5、High 25、Moderate 6、Low 9），见 [完整审查快照](reports/npm-audit-2026-09-18.json)。这些来自本地编译/测试工具依赖，不是 45 个 Solidity 漏洞；不能据此声称工具链安全告警已经清零。没有执行强制批量升级或降级。

`npm audit --omit=dev` 也返回 27 条，其条目路径均位于 Ganache 的嵌套依赖，见 [omit-dev 快照](reports/npm-audit-omit-dev-2026-09-18.json)。不能只凭该开关把这些告警解释为生产合约依赖漏洞，也不能忽略它们。测试仅使用本地临时账户，这套开发运行环境不应直接用作在线签名服务。

## 尚未覆盖的生产边界

1. 真实 PONS Factory/Curve/Hook 的完整接入、版本化费用归集和毕业资产保护仍未完成，见 PONS_INTEGRATION.md。
2. 尚未对目标链、实际股票 Token、真实 Hook、实际流动性和正式权限配置做 fork 测试。模拟 Token 无法涵盖准入、冻结、升级等真实资产行为。
3. 报价签名者负责价格合理性；白名单和签名不能替代独立价格检查。治理和签名者共同失陷不在“普通地址不能提款”的保证范围内。
4. Merkle root 只验证给定权益，持有人完整性、历史余额和最低持仓资格仍依赖链外索引与审核。
5. 未完成独立安全审计、形式化验证或字节码覆盖率测量。测试全绿不能保证代码绝对无问题，也不是允许接收真实资金的生产验收。
