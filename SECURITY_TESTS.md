# 本地安全测试补充

这些测试只运行本地临时 Ganache，使用模拟资产与测试账户，不连接公开网络。

```sh
npm run test:security  # 只运行新增安全用例
npm run check          # 编译、全部测试、本地部署脚本验证
```

## 验证结果

`npm run check` 退出码 0，**50 项全部通过，0 失败、0 跳过**：49 项业务/清单/安全测试，加 1 项完整本地部署测试。其中本轮新增 9 项安全用例，随机状态测试共 144 步。完整输出保存在 [reports/security-check-2026-09-17.log](reports/security-check-2026-09-17.log)。

本轮未复现新的非法资金提取或资金守恒漏洞，因此没有新增生产合约逻辑修复；此前 4 项修复继续通过回归。新增修改集中在测试合约、测试用例和运行脚本。一次测试注册时序问题在测试基础设施中修正，不计作合约漏洞。

## 新增内容

| 用例 | 检查方式 |
|---|---|
| 兑换接口虚报到账 | 白名单测试 adapter 实际转出 quote，却不交付输出 Token；要求整笔交易回滚，收款人不得获利，余额/负债/预算/供应保持原状，同 nonce 的正确交易之后可成功 |
| 兑换接口超额支出 | 尝试多转走 1 个最小单位，核验准确授权阻止支出、交易回滚、无残留额度 |
| 已授权操作员在兑换中重入 | 特意给测试 adapter operator 权限，使用有效内层签名调用；确认重入失败而不是仅被权限检查拦截，内层计划之后仍可独立执行 |
| 成功兑换后的残留授权 | 测试 adapter 再调用 transferFrom，要求失败且池余额、用户余额和负债不变 |
| Token 转账回调重复领取 | 收款合约在 ERC20 转账过程中再次 claim；确认只获得一次权益，另一用户仍能完整领取剩余权益 |
| Token 不执行 burn | 模拟 burn 返回但不减少供应，要求整个回购回滚；恢复正常 burn 后原计划可以重试 |
| 无效签名/参数改变 | 提交错误签名、其他项目、其他版本、其他 nonce；使用实际回滚交易逐一核验余额和预算不变 |
| 随机状态与资金守恒 | 3 个固定种子，各 48 步；两个项目、多个配置版本之间切换入账、收入领取、回购、策略变更和越权分账。每一步用独立账本核对累计比例、项目/版本预算、各池余额与负债，以及总入账 = 池中余额 + 已领取 + 已兑换支出 |

安全测试定义位于 `test/security-cases.mjs`，由现有 fixture 注册并逐项回滚快照；异常行为合约位于 `test/mocks/SecurityMocks.sol`，不属于生产部署产物。随机测试是有限的可复现状态机测试，不等于无限状态证明或完整模糊测试平台。

## 解释结果时的边界

- 对恶意 adapter 的部分测试，特意由治理加入白名单且由签名者签发计划，用来验证资金池自身是否仍然执行预算、到账和重入检查。不能据此认为任意恶意 adapter、恶意资产或错误价格签名都是安全的。
- 分红名单和最低持仓资格仍依赖链外复算与审核者；若审核者签发错误 root，链上不能自动判定其不公平。这是当前明确的信任边界，不由这些测试消除。
- 真实 PONS / V4 接入、真实 Token 特性、索引器和审核服务未在这些模拟测试中验证。
- 发现能够复现的生产代码缺陷应先保留失败用例，再修复并重跑全部测试；不能为了“测试通过”直接移除对应断言。
