# 合约全面测试记录（2026-09-17）

本轮对象是 `contracts/` 中新编写的 OURS 收益模块、分红清单工具与部署脚本。不是已完成生产接入的 PONS 全套发行协议。

## 最新结果：独立平台收入模块

在新的独立仓库 `murphumm-collab/ours-protocol-contracts` 中新增 `OursPlatformTreasury`。完整 `npm run check` 退出码 0：**59 项通过、0 失败、0 跳过**（58 项业务/清单/安全/平台测试 + 1 项部署 CLI 测试，后者运行两个部署脚本）。

新模块 9 项测试包含实际 FeePool 30 → 24/6、80/20 累计入账、策略与保留桶隔离、股票买入、LP 增减和手续费、原生资产、签名/重放、延时与取消。新合约部署后字节码 18,065 字节，低于 EIP-170 上限。两个部署脚本仅在本地临时链演练，未向公开网络部署。

详见 [平台合约说明](PLATFORM_TREASURY.zh-CN.md) 与 [本轮完整输出](reports/platform-check-2026-09-17.log)。以下为保留的历史验证记录；原有真实 PONS / V4 接入和独立审计边界仍然适用。

## 上一阶段：安全测试补充

新增 9 项安全测试后，全量 `npm run check` 退出码 0，**50 项通过、0 失败、0 跳过**（49 项业务/清单/安全测试 + 1 项部署测试）。覆盖异常兑换、重入、无效销毁、残留授权及 3 个固定种子的 144 步资金守恒检查。未发现新的可复现非法提款漏洞，未新增生产合约逻辑修复。

详见 [SECURITY_TESTS.md](SECURITY_TESTS.md) 和 [完整测试输出](reports/security-check-2026-09-17.log)。下文保留首轮验证记录及仍未完成的验证边界。

## 首轮结果

`npm run check` 退出码 0：**41 项通过、0 失败、0 跳过**（40 项业务/清单测试 + 1 项部署 CLI 测试）。原始记录见 [reports/check-2026-09-17.log](reports/check-2026-09-17.log)。

10,000 人清单中的示例用户 proof 为 8 层，本轮实际领取消耗 **135,477 Gas**；未遍历其他持有人。该值不是所有资产、所有树位置的 Gas 上限。

## 执行方法

```sh
cd contracts
npm ci --ignore-scripts
npm run check
npm audit --json
```

`check` 包含编译、业务/异常/规模测试，以及只连接本地 Ganache 的部署 CLI 演练。测试无需真实 RPC 或真实私钥。部署演练自行生成临时测试账户和配置，结束后清理。

环境：Node.js 22；Solidity 0.8.30；OpenZeppelin 5.4.0；ethers 6.17.0；Ganache 7.9.2。optimizer 200、viaIR、EVM target paris；本地业务 EVM 为 Shanghai。Ganache 原生扩展回退 JS，不影响已执行断言。

## 本轮发现并修复

| 问题 | 复现与影响 | 修复 |
|---|---|---|
| 毕业迁移期误阻止独立分红兑换 | Meme 已毕业、外盘尚未绑定时，quote → 股票 Token 也被 `isTrading` 拦截 | 仅兑换包含项目 Meme 时检查其交易阶段；暂停、签名、资产及预算校验保持有效 |
| 不可逆 Pool 绑定可能接错 | Registry 接受另一 Registry 的池，或接受 Fee/Buyback 顺序颠倒 | 一次绑定前同时核验各池所属 Registry 和固定 `POOL_KIND` |
| 分红清单大整数精度损失 | 超过 JS 安全整数范围的 Number 在 BigInt 转换之前已舍入 | 拒绝不安全 Number、非十进制整数及 uint256 越界；零地址自动排除，零合约地址拒绝 |
| 部署脚本连续交易 nonce 冲突 | 本地实际执行 CLI，在第二次部署时出现“account nonce 2, tx nonce 1” | 使用 ethers NonceManager；六合约连续部署及初始化复测通过 |

前三项均先以回归用例复现失败，再修复至通过。部署问题也先由完整 CLI 演练复现，修复后复测。

此外升级 ethers 6.15.0 → 6.17.0，固定 solc 的 tmp 依赖为 0.2.7，处理对应工具依赖告警，不改变 Solidity 编译器版本。

## 覆盖范围

- **分账**：开关关闭 100% 平台；开启 30% 平台、70% 按自定义比例；累计舍入与尾差释放；纯回购、纯分红、纯创建者，以及其他比例组合；各池余额和负债守恒。
- **历史版本**：延时生效、取消不复用版本、旧费旧策略、收款地址冻结、controller 两步交接、跨项目隔离。
- **兑换**：签名及 nonce、过期、参数/route 篡改、超预算、输出不足、回滚后重试、白名单撤销、单批上限、ERC1271、暂停、实际余额差与部分退款、burn。
- **分红**：门槛包含等值、排除地址、空期、按期库存、近期快照哈希、重复登记/建期、审核等待与取消、不可修改的 Active root、超额发布/领取、重复/冒领、受阻收款人不影响其他用户、暂停后仍可领取。
- **资金行为**：ERC20/原生资产、税费代币拒绝、原生领取重入、外部捐赠不计入可支配预算、授权额度清零。
- **规模与清单**：65 组不同人数的生成式持仓测试，覆盖奇偶树、全部 proof、输入顺序无关、舍入守恒；10,000 名合格持有人的清单生成并实际单人链上领取。
- **部署**：本地 HTTP RPC 下运行真实部署 CLI，检查六个合约代码、Pool 绑定、资产/adapter/operator 白名单、待接受治理权；错误 chainId 在发交易前拒绝。

万人测试不是同时执行一万笔领取交易。它生成完整一万人权益树，并验证其中一位用户提交自己的 proof 即可领取，合约不遍历其他持有人；Gas 为本地 mock ERC20 下的实测，真实股票 Token 的转账逻辑会影响 Gas。

## 编译产物

| 合约 | 部署后代码字节数 |
|---|---:|
| OursProjectRegistry | 10,497 |
| OursFeePool | 11,435 |
| OursBuybackPool | 8,675 |
| OursDividendPool | 13,972 |
| OursCurveAdapter | 2,930 |
| OursV4Adapter | 5,261 |

均低于 EIP-170 的 24,576 字节上限。`OursFeeAccrual` 是抽象接入组件，不是第七个可单独部署的业务合约。编译/部署/清单脚本另通过 Node 语法检查。

## 依赖审查结果

最终 `npm audit --json` 返回非零：33 个依赖条目告警（Critical 5、High 22、Moderate 5、Low 1），路径全部属于 Ganache 或其嵌套依赖。审计快照保存于 `reports/npm-audit-2026-09-17.json`。

这是测试工具链尚未清零的依赖风险，不是 33 个 Solidity 漏洞。没有运行自动强制降级 Ganache 的 `npm audit fix --force`。当前仅将它用于本地隔离测试账户；生产服务不能将这个测试运行环境直接当作在线签名或 RPC 服务。后续应替换/升级本地 EVM 工具并重新跑回归。

## 尚未验证的边界

1. **真实 PONS 接入仍未完成**：MockFactory/MockCurve 不能证明发行、曲线数学、真实费用扣除、毕业储备保留和原版权限改造正确。具体工作见 `PONS_INTEGRATION.md`。
2. **真实 Uniswap V4 fork 未运行**：MockV4Manager 只验证适配器控制流，尚未验证真实流动性、Hook 收费、价格边界、目标链 EVM 与正式合约字节码。
3. **链外快照存在审核信任**：Merkle 验证不独立证明完整持有人集合、历史余额、最低持仓或快照块恰为期末；这些由索引器/审核者校验。未实现实际索引与对账后台。
4. **未运行正式网络部署、前端钱包端到端联调、独立安全审计、形式化验证或字节码覆盖率测量**。本地全绿不能替代这些步骤。
5. 已启用期次的未领款和舍入余款不能被管理员提走；审核者有发布不公平 root 的信任风险，当前设计的金额上限只能防超支。

