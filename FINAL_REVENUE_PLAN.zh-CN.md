# OURS 收益模块最终业务方案与整合要求

日期：2026-09-18。用户已明确采用平台收入固定 **50/20/10/20**；股票流动性由平台手动创建，LP 不销毁。本文定义后续实现目标，取代旧平台说明中“统一 80% 策略预算、回购平台币可保留并提款”的业务规则。**业务方案已确认，不表示当前任一分支已经完成该方案或可部署。**

后续合并执行遵循用户要求：直接使用现有 `codex/contracts`，不再创建整合分支。四桶代码、尾差修复和两项安全保护已在本次工作中统一，结果见 [合并记录](MERGE_VALIDATION.zh-CN.md)。下节固定提交对比保留为合并前的历史证据。

## 1. 合并前分支核查

| 分支 | 本次固定提交 | 可复用内容 | 未完成事项 |
|---|---|---|---|
| codex/contracts | 8f44c93 | 项目收益模块、官方 V4 本地集成测试、股票资格撤销保护、禁止放弃 Treasury 治理权 | 平台仍为旧 80/20 统一策略桶，须换成四个用途桶 |
| codex/audit-fixes-2026-09-18 | e22f80a | 四个用途桶、平台回购真实销毁、奖励出口、更多角色与异常行为测试 | 舍入下溢未修复；缺生产奖励分配器；缺上述两项保护 |

e22f80a 相对 cb0f381 仅修改文档和 MockV4Manager，没有修改生产 Solidity。本次用 e22f80a 未修改的 Treasury、solc 0.8.30 和本地 Hardhat EVM 独立确认：先入账 9，再领取 1，或手工存入 1/2，均出现 Panic(0x11)；实际失败领取完整回滚；待领取累计至 3 后成功，总入账变为 12。记录见 reports/latest-branch-rounding-2026-09-18.json。

此前的 77 项测试属于 codex/contracts；新分支文档声称的 76 项不是本次独立全量复跑结果。两套数量不能相加，也不能证明合并后的代码正确。整合应以四桶规则为业务目标，逐项迁入验证过的保护与测试，重新完成回归，不能直接用任一分支上线。

## 2. 资金流与比例

```text
发行 → 内盘 Curve / 外盘 Hook 收取真实交易费
     → 按项目、币种、策略版本记录费用
     → FeePool 归集，必要时受限兑换为报价资产
         ├─ 自定义关闭：100% 归平台
         └─ 自定义开启：30% 归平台，70% 按创建者配置分配
                          ├─ Meme 回购池：回购项目币并 burn
                          ├─ 项目分红池：取得分红 Token，用户自己 claim
                          └─ 创建者收入：创建者自己领取

平台实际收入 → PlatformTreasury
                ├─ 50%：回购平台 Token 并真实 burn
                ├─ 20%：股票流动性专用预算，平台手动建仓
                ├─ 10%：贡献奖励专用预算 → 平台 Token → 独立奖励分配器
                └─ 20%：运营收入 → 固定平台收款地址
```

50/20/10/20 的分母是 Treasury 实际收到的平台 fee，不是总交易额，也不是总交易费。自定义开启时，总交易费 100 对应项目 70、平台回购 15、平台流动性 6、贡献奖励 3、运营 6，忽略最小单位舍入。关闭时总交易费 100 全归平台，再按 50/20/10/20 分配。

创建者可以配置其 70% 中回购、分红、直接收入的比例，三项合计 100%；不能配置平台四桶比例。策略修改延时生效，历史费用按产生时版本处理，不能用新收款人覆盖旧权益。

发行费、Gas、额外 creatorTax、DEX LP 手续费不自动等同于这里的交易 fee。首版关闭旧 creatorTax 和旧回购分配路径，避免重复扣费。发行费若也要进入四桶，必须经单独明确的认证入账接口；直接转账不自动计入预算。

## 3. 合约职责及资产保管

| 组件 | 职责 | 资金边界 |
|---|---|---|
| PONS Factory/Curve/Hook + FeeAccrual | 发行注册、真实收费、版本化费用累计、毕业绑定 | 未分配费用不得混入交易本金或毕业 LP；真实接入尚待实现 |
| ProjectRegistry | 项目认证、策略历史、角色及白名单 | 不托管策略资金 |
| FeePool | 归集、必要的费用币兑换、30/70 分账 | 只能接收认证来源；项目预算与平台收入隔离 |
| BuybackPool | 项目 Meme 回购与真实销毁 | 不使用项目分红、其他项目或平台资金 |
| DividendPool | 项目分红资产取得、周期库存、Merkle 权益领取 | 已激活权益不能由管理者提走或改写 |
| CurveAdapter / V4Adapter | 限定路由、实际到账、退款、原生币结算 | 不允许任意 router calldata；不得留下多余额度授权 |
| PlatformTreasury | 固定四桶、平台币销毁、股票 LP、运营收入、奖励资金交付 | 四桶不可串用；删除旧通用策略提款与“保留平台币后提款”路径 |
| PlatformRewardDistributor（待实现） | 接收奖励专用平台币，发布贡献奖励权益，用户领取 | 独立于项目持币分红，不能挪用项目资金；不能用 Mock 或 DividendPool 代替 |

**平台 Token 的处理**：50% 桶购买所得销毁；若收入本身就是平台 Token，可直接从该桶 burn，并核对余额和 totalSupply 同时减少。20% 流动性桶中的平台 Token 用于 LP，不销毁；10% 奖励桶中的平台 Token 发给合格贡献者，不销毁。

**股票 LP**：平台人工选择资产、配对、初始价格、金额和区间，治理/授权操作员提交受限计划。收入入账、部署和普通用户交易均不自动建仓。仓位由 Treasury 直接持有，不转给操作员或黑洞地址。可单独收取手续费；减少/退出仓位须治理排队并等待延时，回收资产回到流动性桶。LP 不销毁不等于永久锁死，允许重新配置流动性。LP 手续费归流动性桶，不再次进行四桶分配。

**项目分红**：只提供用户主动领取，不循环持有人或空投。最低持仓由项目创建者配置并纳入每期清单；创建者持币时按相同规则参与，也可以领取自己策略中的直接收入。最低持仓资格与完整地址集合需链外审核，Merkle proof 不独立证明历史余额。当前周期口径为分红资产实际到账周期，不承诺按原交易发生周期归属；快照规则和领取页面须明确显示这一点。

**贡献奖励分配器建议接口与安全边界**：按奖励期拉取 Treasury 的可用奖励平台币，以实际到账记账；登记清单哈希、Merkle root 和总权益，审核等待后激活。叶子绑定 chainId、分配器、期次、用户和数量；用户只能领取自己的权益，防重复、超额和重入。激活后的 root 不可修改，未领取权益不可被管理员回收。分配器应允许两步治理移交，禁止放弃必要治理权；其地址若在 Treasury 中不可变，须先完成部署和端到端拉款/领取演练。贡献积分来源、反刷规则、周期和审核延时属于尚待配置的产品参数，不应硬编码未经确认的奖励名单。

## 4. 四桶整数核算必须修复

对每个收入币种分别记录累计收入 G。计算四个独立单调的累计目标：

```text
burnTotal      = floor(G × 5000 / 10000)
liquidityTotal = floor(G × 2000 / 10000)
rewardTotal    = floor(G × 1000 / 10000)
operatingTotal = floor(G × 2000 / 10000)
dust           = G - 四个累计目标之和
每桶本批新增额度 = 新累计目标 - 该桶上次累计目标
```

使用 Math.mulDiv，避免先乘后除溢出。每个累计目标单调递增，累计已分配值不因兑换、销毁、领取或退出而减少。尾差单独记账，不进入任一可花费桶；后续新收入和旧尾差共同补足分配增量。本批四桶增量可能超过本批收入，差额来自旧尾差，不能误判为超支。

例如 G 从 9 到 10：四桶从 4/1/0/1、尾差 3，变为 5/2/1/2、尾差 0；新增 1 配合原尾差 3，新增四桶额度合计 4。禁止把尾差提前计入可销毁额度，也禁止用 unchecked 或把负增量强行置零掩盖错误。

流动资产核对：合约实际余额应覆盖四个当前余额桶及当前尾差；直接捐赠另外隔离。已投入 LP 的资产不再作为合约内可用现金重复计算，独立记录 positionLiquidity。支出前后核验实际余额变化，不只相信外部返回值。

## 5. 权限、Gas 和简单后台

- 项目费用归集、分账、回购、取得分红资产：平台授权 operator 或该项目 controller 执行；兑换还必须有有效报价签名和链上预算、路由、限额、时效校验。不会自动把 PONS 原版 feeSweepOperator 权限解释成人人可用。
- 平台四桶的交易和 LP 管理由平台治理/操作员执行，项目创建者不能动用平台预算。收款地址固定的运营领取和已批准的延时 LP 退出可以开放触发，触发者不能改变去向。
- 治理使用多签，报价签名和分红审核分权；签名私钥不放数据库或网页。暂停阻止新增兑换/建仓，不阻止已归属用户权益的领取。保留 Treasury 禁止放弃治理权、执行时重查股票资格、V4 原生结算先 sync 三项保护。
- 谁发交易谁支付 Gas：平台操作平台付，创建者操作创建者付，用户领取用户付。首版无池内 Gas 报销、keeper 奖励或隐含代付。

后台采用一个代码库、两个进程、一个 PostgreSQL 即可：

1. **API/索引进程**：同步事件和确认块，记录项目/策略、费用、桶余额、执行状态、LP、分红期和领取情况；处理链重组；为前端提供清单、proof 和交易状态。链上账本是资金依据，数据库不是提款授权。
2. **任务进程**：归集费用、准备兑换报价、构造签名计划、提交与追踪交易、生成分红/贡献奖励清单并等待审核。用数据库任务队列即可，无需先拆微服务；按链和发送账户管理 nonce，唯一任务键防重复，对不确定回执先查链再重试。

平台后台提供手动 LP 页面：配对/区间/金额预览、签名与交易确认、现有仓位、手续费、退出排队和取消。任务进程不自行决定建仓。报价服务需独立价格和滑点检查，链上有效签名不意味着成交价格合理。

## 6. 整合与上线验收顺序

1. 直接在现有 `codex/contracts` 合并四桶规则、尾差修复、两项保护与官方 V4 测试；同步 ABI、EIP-712、部署脚本和文档。后台尚未实现，后续签名服务必须使用本版 `purpose` 类型，不能沿用旧 `retainOutput`。
2. 实现并测试生产 RewardDistributor；固定可编译的 PONS 完整源码版本，接入发行、费用储备、毕业和 Hook。清理旧收费与回购路径；内部兑换是否重复收费必须通过认证来源规则明确实现。
3. 完成四桶边界回归：连续最小单位、9 后加 1/2/3、大数尾数、不同 decimals、原生币、已 burn 后再入账、尾差释放、分批/合批等价、不同资产隔离、四桶余额和 LP 守恒。奖励测试覆盖冒领、重复领取、错误清单、冻结后重试、审核轮换和余额不足。
4. 运行整合后的完整业务/安全/部署测试，以及官方 V4 核心 LP、Hook 和结算测试。任何已知失败都不能用增加通过数量代替修复。
5. 用明确目标链 RPC、正式 PoolManager/Hook/股票 Token 做 fork 验证，覆盖准入/冻结限制、毕业失败重试、费用不混入迁池本金、首购与末笔部分成交；补后台与前端钱包联调。
6. 确定部署参数，独立安全审查、测试网演练、多签角色核验，再决定正式部署。当前没有执行公开链部署，不能以业务规则确认代替上线验收。

部署前还须填写：目标链、完整 PONS 版本、平台 Token 及 burn 能力、股票资产/交易对、治理与运营收款人、报价签名者/审核者、各延时和限额、贡献奖励规则。它们不改变已经确认的 30/70 与 50/20/10/20，也不改变“平台手动建 LP、LP 不销毁、分红用户自领”。
