# 原分支合并与分账修复

日期：2026-09-18。按用户要求，直接把 `codex/audit-fixes-2026-09-18` 的 `e22f80a785fd615ddc9fcf349ae93161cf8e4a71` 合入现有 `codex/contracts`，没有新建分支。合并前目标分支为 `55c0f2f`。保留 Git 两条历史，以一个合并提交统一后续维护。

## 本次修改

- 平台实际 fee 固定拆为 50% 平台币回购销毁、20% 股票流动性、10% 贡献奖励、20% 运营收入。项目层 30/70 和关闭自定义时 100% 平台的规则保持原义。
- 删除旧 `retainOutput`、保留后通用提款等接口；新签名 `SwapPlan.purpose` 为 0/1/2 三种明确用途，构造和部署配置增加固定 `rewardDistributor`。三个策略账本不能提到运营国库。现有后台如按旧 ABI 接入，必须更新；本仓库尚未实现后台。
- 保留并整合执行时股票资格重检、Treasury 禁止放弃治理权、V4 原生币结算前同步币种三项保护。
- LP 继续由平台人工决定建仓、Treasury 持有，不销毁或转入黑洞；已有 LP 延时退出回到流动性桶，手续费也进入该桶。
- 原有深入测试不直接删除：治理退出等测试改为新版本 LP 队列语义；原保留 Token 账本状态机改为四桶、实际 burn、奖励交付和 LP 的独立账本模型。三个种子各 60 步，仍逐步校验供应和资金守恒。

## 确认问题后再修复

合并前对固定提交 e22f80a 的原始生产源码做本地 EVM 复现，记录见 [原始结果](reports/latest-branch-rounding-2026-09-18.json)。先收 9，再收 1 或补入 1/2，均出现 Panic(0x11)。实际失败交易回滚，不是直接盗币。

原因是多个向下取整的差值不保证单调：旧销毁累计额度从 G=9 时的 6 降到 G=10 时的 5，新增额度计算发生下溢。

修复为四桶分别计算累计目标 `floor(G×比例)`，四项之外的零头记录为 `unallocatedDust`。新入账可释放旧零头，但零头不能被直接花掉；当前可用余额和累计已分配金额分开，已销毁/领取不会重置累计值。`accountedBalance` 纳入零头，`RevenueDustUpdated` 记录变化。没有使用 unchecked 或强行截断负值。

新增 11 项边界回归：原生币/ERC20 从 9 连续逐单位到 40、9 后加 2/3、真实 FeePool 9→10、已经 burn/领取运营收入后再入账、0/6/18 位小数、大额尾数 9、接近 uint256 上限、拆分与合并入账等价、币种间尾差隔离。

## 验证方式

```sh
npm run test:rounding
npm run check
```

完整回归包含编译、所有清单/项目/平台/安全测试、两个本地部署 CLI 演练及官方 V4 核心集成。Ganache 用于业务/异常与状态机测试；未修改的官方 V4 PoolManager 在本地 Cancun EVM 运行。没有公开链交易。

`npm run check` 退出码 **0**，合并后实际运行 **105 项通过、0 失败、0 跳过**：94 项业务/清单/安全/平台测试，1 项同时演练两个部署脚本的 CLI 测试，以及 10 项官方 V4 核心/托管回归。没有把两个父分支的结果相加，也没有把此前单独运行的 11 项尾差测试和 10 项 V4 测试重复计数。完整输出见 [合并回归日志](reports/merged-check-2026-09-18.log)。

原项目状态机 144 步与新的平台四桶状态机 180 步均包含在全量回归中，共 324 步。本轮不是完整形式化验证或无限状态模糊测试。新增/修改的 JS 文件通过语法检查，合并文件通过 `git diff --check`。

生产部署后代码：Treasury **20,285 bytes**、V4Adapter **5,258 bytes**，均低于 EIP-170 的 24,576 bytes 上限。始终回滚的 `renounceOwnership` 保留编译器“可限制为 view”的建议；Ganache 原生扩展回退 JS，未影响本轮断言。

合并开始于香港时间 2026-09-18 11:18，全量回归于 11:28 完成，约 10 分钟；其后整理报告和提交。源码尚未向公开链部署。

## 尚未完成的范围

1. 生产贡献奖励分配合约尚未实现。奖励出口与 mock 的交付、重入、冻结和记账测试不能证明用户积分/奖励领取已经可用；不能用 mock 或任意有代码地址部署正式 Treasury。
2. 完整真实 PONS Factory/Curve/Hook、毕业储备及费用隔离、真实目标链 fork、真实股票资产限制、后台和前端钱包联调仍待完成。
3. 没有执行正式部署、独立第三方审计或形式化验证。已有开发工具依赖告警见 reports/npm-audit-2026-09-18.json，本次未更改工具依赖或声称清零。
4. 单次合并通过不是绝对安全证明；目标链地址、分配器、权限与价格签名服务仍需验收。
